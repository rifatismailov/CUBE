<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Колір фону -->
    <solid android:color="@color/blue" />

    <!-- Рамка (опціонально) -->
    <stroke
        android:width="2dp"
        android:color="@color/white" />

    <!-- Відступи (опціонально) -->
    <padding
        android:left="2dp"
        android:top="2dp"
        android:right="2dp"
        android:bottom="2dp" />
</shape>
